import 'package:flutter/material.dart';

class ApprovalsTab extends StatelessWidget {
  const ApprovalsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Approvals Page", style: TextStyle(fontSize: 22)),
    );
  }
}
