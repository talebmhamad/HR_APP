import 'package:flutter/material.dart';

class TimeClockTab extends StatelessWidget {
  const TimeClockTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("TimeClock Page", style: TextStyle(fontSize: 22)),
    );
  }
}
