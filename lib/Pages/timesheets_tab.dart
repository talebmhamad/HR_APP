import 'package:flutter/material.dart';

class TimesheetsTab extends StatelessWidget {
  const TimesheetsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Timesheets Page", style: TextStyle(fontSize: 22)),
    );
  }
}
